# Completion checklist
- Run syntax checks for changed JavaScript files with `node --check`.
- If UI or runtime behavior changed, verify manually in Chrome on a YouTube watch page.
- Update README and design docs when behavior changes materially.
- Do not claim completion without fresh verification evidence.